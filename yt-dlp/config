-o %(upload_date)s-%(title)s-%(id)s.%(ext)s
--windows-filenames
#--trim-filenames 32 

# Добавляю время скачивания, чтобы после закидывания на телефон сортировалось в
# приложении для аудиокниг по имени файлов как по времению добавления:
#--exec "mv {} `date '+%Y%m%d'`_{}"
